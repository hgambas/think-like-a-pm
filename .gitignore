# Unshipped skills and design explorations — public when each one ships
workshop/

# Local tool caches, not part of the plugin
.omc/
.impeccable/
.DS_Store

# Installed dependencies. Only the optional MCP server needs any, and anyone who wants it
# runs `npm install` themselves — 24MB of other people's code has no business in here.
node_modules/
